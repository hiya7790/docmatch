"""
Plugins package — Bring Your Own Model (BYOM) plugin system.

Place Python scripts in this directory that define classes inheriting
from `models.extraction.base.ExtractionStrategy`. The StrategyFactory
will automatically discover and register them on startup.

See `example_plugin.py` for a template.
"""
